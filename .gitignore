# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env / secrets — keep .env.example tracked as the template
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scraper tool — standalone, with its own deps and large generated caches
# (squads.json, wikipedia.cache.html, photos.cache.json). Run `bun run scrape`
# in scraper/ to regenerate.
scraper/

# NOTE: src/data/worldCup2026.ts is intentionally TRACKED — it's a build-time
# source dependency (static teams/matches/stadiums the app imports), not
# disposable scraper output. Do not ignore it or fresh-clone / CI builds break.
